"use client";

/**
 * LetrasSection
 * ───────────────────────────────────────────────────────────────────────────
 * `EditorLetras.tsx` (export default `EstudioLetras`) ya es autocontenido.
 */

import EstudioLetras from "@/features/editorGarlia/views/EditorLetras";

export function LetrasSection() {
  return <EstudioLetras />;
}
