/**
 * PerfilTemplate — plantilla base para páginas de perfil de usuario.
 * Actualmente sin implementar. Usar cuando se construya la vista de perfil público.
 */
export default function PerfilTemplate() {
  return null;
}