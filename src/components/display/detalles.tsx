/**
 * DEPRECADO — este wrapper ya no es necesario.
 * DetalleMaestro maneja su propio estado isOpen internamente.
 *
 * Migración: reemplazar imports de "@/components/display/detalles"
 * por "@/components/display/DetalleMaestro/DetalleMaestro"
 *
 * Este re-export existe solo para no romper imports existentes.
 */
export { default } from "@/components/display/DetalleMaestro/DetalleMaestro";